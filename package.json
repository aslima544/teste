{
  "name": "sistema-gestao-consultorios",
  "version": "1.0.0",
  "description": "Sistema de Gestão de Consultórios - Backend e Frontend",
  "scripts": {
    "start": "uvicorn backend.server:app --host 0.0.0.0 --port $PORT",
    "dev": "uvicorn backend.server:app --reload --host 0.0.0.0 --port 8000"
  },
  "dependencies": {},
  "engines": {
    "python": "3.11"
  }
}